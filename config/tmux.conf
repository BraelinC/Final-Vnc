# tmux configuration for Guacamole SSH terminal
# Copy this to ~/.tmux.conf on the VPS for each user

# Enable mouse support (required for scroll to work)
set -g mouse on

# Increase scrollback history
set -g history-limit 50000

# Better colors
set -g default-terminal "screen-256color"
